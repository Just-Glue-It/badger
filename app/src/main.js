import Cycle from '@cycle/core';
import {makeDOMDriver} from'@cycle/dom';

// TODO: create components and drivers
import components from './components';
// import drivers from ...

const main = components;

run(main, {
  // DOM: ...
});
